services:
  frontend:
    image: nginx:1.27-alpine
    container_name: ezulhreasqkpvbct-frontend
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
